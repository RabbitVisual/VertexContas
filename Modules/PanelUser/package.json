{
  "private": true,
  "name": "@vertex/paneluser",
  "version": "1.0.0",
  "description": "Module PanelUser - Uses root Vite build. Run 'npm run build' from project root.",
  "scripts": {
    "build": "echo \"PanelUser uses root build. Run: cd ../.. && npm run build\" && exit 0",
    "dev": "echo \"PanelUser uses root Vite. Run: cd ../.. && npm run dev\" && exit 0"
  }
}
